name: Publish Release

on:
  workflow_run:
    workflows:
      - "WezTerm Windows Alt Icon Builds"
    types:
      - completed

jobs:
  on-success:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: |
          echo "Workflow Success:"
          echo "${{ toJson(github.event.workflow_run) }}"
